# 9. Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}

# No, you cannot change the values inside a list that is contained in a set — because lists are mutable (changeable) and sets can only contain immutable (hashable) elements.